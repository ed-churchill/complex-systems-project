set=[]
for x in range(1,200):
    temp=1
    while temp==1:
        temp=input("vertices adjacent to "+str(x)+":  ")
        if not temp:
            temp=0
        else:
            set.append([x,int(temp)])
            temp=1
print(set)

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

%Vertex set
V=[200]

%taxiedgeset
E_t =  
